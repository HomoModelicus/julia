

include("../src/Stacks.jl")


# #=
module stest
using ..Stacks




stack = Stack{Int}()

push!(stack, 10)
push!(stack, 20)

push!(stack, 30)
# push!(stack, 40)

# push!(stack, 50)
# push!(stack, 60)
# push!(stack, 70)
# push!(stack, 80)

# push!(stack, 90)


# pop!(stack)



end
# =#
